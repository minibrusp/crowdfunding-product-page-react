import Nav from "./Nav";

export default function Header() {
  return (
      <header className="bg-mobile-hero h-[300px] bg-cover py-8 px-6 font-commissioner">
        <Nav />
      </header>

  )
}
